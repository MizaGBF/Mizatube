javascript:(function () { let copyListener = event => { document.removeEventListener("copy", copyListener, true); event.preventDefault(); let clipboardData = event.clipboardData; clipboardData.clearData(); let sk; try{sk=Object.values(Game.view.subaction._byId)[0].attributes.list;}catch(err){sk=Game.view.jobDetailData.abilityList;}; clipboardData.setData("text/plain", JSON.stringify(sk)); }; document.addEventListener("copy", copyListener, true); document.execCommand("copy"); })();